<div class="hero">
  <div class="inner">
    <% if (!page.title) { %>
    <div class="hero-img">
      <img src="https://avatars0.githubusercontent.com/u/6768662?v=3&amp;s=200" alt="Papi">
    </div>
    <% } %>
    <div class="hero-text">
      <% if (page.title) { %>
        <h1><%- page.main ? page.main : page.title %><%- page.type === 'properties' ? ' property' : '' %></h1>
      <% } else { %>
        <h1><%- config.title %></h1>
        <p>A great way to work with custom fields and page types in WordPress, <a href="/docs/">learn more</a></p>
      <% } %>
    </div>
  </div>
</div>
